// version.zig — Single source of truth for the version string
// Keeping it separate means we only update it in one place.
// Sounds obvious, but you'd be surprised how many projects get this wrong.

pub const VERSION = "0.1.0";
pub const BUILD_DATE = "2025";
pub const AUTHOR = "zigscanner contributors";
pub const LICENSE = "MIT";
